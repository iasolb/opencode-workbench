---
description: Structure and stubs ONLY, never implementations. Working code must have been RUN against real inputs.
---

Scaffold: 

Create the declared structure and stubs only. Never implement. The test for
keeping working code is whether it has been RUN against real inputs. What counts
as "necessary" is declared in this command's own doc.
